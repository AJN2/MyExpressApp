doctype html
html(lang='en')
  include ../common/head.pug
  +head("MyNodeJS")
block content
    h1 Lead Display
    table
        tr
            td
                <span> #{lead.id}</span>
            td
                <span> #{lead.email}</span>
            td
                <span> #{lead.name}</span>
            td
                a(href="/lead/" + lead.id + "/edit" ) Edit